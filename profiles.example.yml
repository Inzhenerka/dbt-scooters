default:
  target: dev
  outputs:
    dev:
      type: postgres
      host: dpg-crd3cll2ng1s73fnn1eg-a.frankfurt-postgres.render.com
      port: 5432
      user: admin
      password: "{{ env_var('DBT_ENV_SECRET_PASSWORD') }}"
      dbname: dev_f0vi
      schema: dbt
      threads: 4
